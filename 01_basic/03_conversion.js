let score = "100";

 console.log( score);
let newValue = Number(score);
console.log(newValue);

console.table([typeof(score), typeof(newValue),]);

let loggedStatus = "";

let newStatus = Boolean(loggedStatus);

console.log(typeof(newStatus));
